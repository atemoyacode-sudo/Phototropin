# バグ調査・再検証記録（2026-09-05）

調査時点の未コミット変更を含む作業ツリーを対象とし、既存変更を保持した。
既存27テストは成功した。以下の4件は、テスト用の依存注入だけを追加した修正前コードで失敗を確認し、挙動修正後に同じ再現テストが成功した。

| 優先度 | 確認したバグ | 原因と修正 | 再現・回帰テスト |
|---|---|---|---|
| P1 | 録音中に監視画像の処理が開始すると停止操作が拒否される。90秒タイマーも同じ停止メソッドを呼ぶため、画像のOCR・モデル応答を待つ間は停止できない。 | `isBusy`だけを見て画像処理を始めていた。録音中は画像を待機させ、手動の画像説明・撮影・音声への再質問も開始しない。停止後に待機画像を処理する。 | `testRecordingDefersScreenshotsAndCanStopImmediately`：修正前は停止直後も録音フラグがtrue。修正後は即座にfalseになり、後続画像も回答される。 |
| P2 | 範囲撮影を連続操作すると2つの選択プロセスが開始できる。まだファイルがないため、同一秒の操作は同じ出力先にもなり得る。 | 範囲選択開始前にbusy状態を確保し、成功・キャンセル双方で解除して待機処理を再開する。 | `testAreaCaptureReservesBusyStateUntilCanceled`：修正前はcapture呼び出し2回、修正後は1回。選択中に追加した画像はキャンセル後に処理される。 |
| P2 | モデル未選択時のスクリーンショットが、モデル一覧更新後も処理されない。 | 待機配列全体を消しながら既読集合に残していた。保存済み画像は待機配列に保持し、モデル取得成功後に処理を再開する。保存しない一時画像は従来どおり削除する。 | `testScreenshotWaitsForModelRefreshInsteadOfBeingLost`：修正前はOCRと回答が空。修正後は再スキャン不要で回答される。 |
| P2 | OCRの読み順が、同じ検出結果の列挙順によって変わる。 | 2要素の高さ許容差で行内・行間を判定する比較関数が非推移的だった。厳密な幾何ソート後、固定の行アンカーでグループ化し、各行を左から右へ並べる。 | `testOCRReadingOrderDoesNotDependOnObservationOrder`：3要素の6順列を比較。修正前は複数の読み順、修正後はすべて同じ。 |

## 採用しなかった候補

透過PNGのJPEG変換時に背景色が保証されていない可能性を調べたが、実際の変換・再デコードで中央画素は白となった。この環境では再現しなかったためバグと認定せず、画像変換コードは変更していない。IPv6 loopbackも既存テストが成功したため変更しない。

## 検証

- `swift test`：34テスト成功（Core 28、AppModel 6）。アプリ・CLIもビルド成功。
- 回帰テストは固有のUserDefaults suiteと一時フォルダを使用する。実際の録音・範囲撮影・オーバーレイは実行せず、LM Studio通信はURLProtocolで模擬する。
- 生成したPNGを監視フォルダで検出し、実際のApple Vision OCR、LM Studio互換の模擬応答、AppModelへの回答反映まで成功。
- 保存しない撮影について、正常処理後とモデル未選択時の削除を検証した。

実機の画面選択UI、録音権限、90秒の実録音、オーバーレイ表示、実LM Studioのモデル品質・応答時間は今回の検証範囲に含まない。全コードに不具合がないことを保証する調査ではない。
